apiVersion: v1
kind: ConfigMap
metadata:
  name: opsmx-provider-config1
data:
  providerConfig: |
    application: testanalysis3
    user: admin
    gateUrl: 'https://isd-argocd.isd-dev.opsmx.net/'
    lifetimeMinutes: 3
    intervalTime: 3
    lookBackType: sliding
    gitops: true
    passScore: 90
    serviceList:
    - metricScopeVariables: "${namespace_key},${pod_key},${app_name}"
      baselineMetricScope: "argocd-gitpr,{{env.STABLE_POD_HASH}},testanalysis3"
      canaryMetricScope: "argocd-gitpr,{{env.LATEST_POD_HASH}},testanalysis3"
      metricTemplateName: "metrixtemplates"
    - logTemplateName: testlog1123
      logScopeVariables: "kubernetes.pod_name"
      baselineLogScope: ".*{{env.STABLE_POD_HASH}}.*"
      canaryLogScope: ".*{{env.LATEST_POD_HASH}}.*"
      logTemplateVersion: "v1.0"
